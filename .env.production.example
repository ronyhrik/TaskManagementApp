# Copy to .env.production. Used by Android's `prod` flavor
# (npx react-native run-android --mode prodDebug, or ./gradlew assembleProdRelease).

APP_ENV=production
API_URL=https://api.taskmanager.com
ENABLE_LOGS=false
