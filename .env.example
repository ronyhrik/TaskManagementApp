# Copy this file to .env to run the default (development) build.
# See ENVIRONMENTS.md for how each variable maps to Android product flavors
# and why iOS currently only reads this base file (per-environment iOS builds
# were out of scope for this assignment).

APP_ENV=development
API_URL=https://dev.api.taskmanager.com
ENABLE_LOGS=true
